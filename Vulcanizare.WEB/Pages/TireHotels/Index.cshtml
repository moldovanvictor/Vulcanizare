@page
@model Vulcanizare.WEB.Pages.TireHotels.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<div class="d-flex justify-content-between">
    <p>
        <a asp-page="Create">Create New</a>
    </p>
    <p>
        <a href="javascript:history.back()">Back</a>
    </p>
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TireHotel[0].TireStatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TireHotel[0].User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TireHotel) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TireStatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Email)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
